import math from "./math";

test('Test the math functions', () => {
    console.log('Hello World');
    // expect(math.sum(1,2)).toBe(3);
    // expect(math.divide(10,5)).toBe(2);
    // expect(math.takeaway(100,25)).toBe(75);
    expect (math.multiply(8,7)).toBe(56);

});